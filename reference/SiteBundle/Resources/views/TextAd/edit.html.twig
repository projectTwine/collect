{% extends 'ClassCentralSiteBundle::base.html.twig' %}

{% block content -%}
    <h1>TextAd edit</h1>

    <form action="{{ path('textad_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('textad') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
